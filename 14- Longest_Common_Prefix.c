char* longestCommonPrefix(char** strs, int strsSize) {
    int i = 0;
    int j;
    char *result;
    int count = 0;
    while (i < strsSize)
    {
        j = 0;
        while  (strs[i][j])
        {
            if (strs[0][j] == strs[i])
        }
    }
}